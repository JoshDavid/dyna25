 GetTreeImage←{
 ⍝ ⍵ ←→ Tree name (scientific) e.g. Quercus bicolor
     ⍝ u←'https://upload.wikimedia.org/wikipedia/commons/c/c3/Quercus_bicolor_morton_acc_71-69-2.jpg'
     u←GetMediaFile ⍵
     r←HttpCommand.Get u
     ~r.IsOK:r.msg
     f←##.Dir,('-'@(' '∘=)⍵),'.jpg'
     t←f ⎕NCREATE(⍠'IfExists' 'Replace')0   ⍝ 0 = Next available tie number
     _←r.Data ⎕NAPPEND t
     _←⎕NUNTIE t
     f
 }
